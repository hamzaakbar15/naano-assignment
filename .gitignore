# Per-session scratch state for the .claude/hooks/capture.js log hook
# (current-model tracking + turn counters). Not part of the submission log;
# .agent-logs/ itself is intentionally NOT ignored - it ships with the repo.
.claude/hooks/.state/

# Personal Claude Code permission overrides for this project - not a team policy.
.claude/settings.local.json

# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (all, including real secrets - only .env.example is committed)
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
